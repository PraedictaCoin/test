<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <meta name="theme-color" content="#7C3AED">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="PRAEDICTA - The prediction game where you test your foresight with virtual PRAE tokens. Crypto, weather, sports, politics, and more. Auto-resolved by 46+ APIs.">
    <meta property="og:title" content="PRAEDICTA – For Those Who See">
    <meta property="og:description" content="Test your predictions with virtual PRAE tokens. Auto-resolved by 46+ APIs.">
    <meta property="og:image" content="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><circle cx='50' cy='50' r='50' fill='%237C3AED'/><text x='50' y='67' text-anchor='middle' fill='%23F0F0F5' font-size='50' font-weight='bold'>P</text></svg>">
    <meta property="og:url" content="https://praedictacoin.github.io/test/">
    <meta property="og:type" content="website">
    <meta name="twitter:card" content="summary">
    <meta name="twitter:title" content="PRAEDICTA – For Those Who See">
    <meta name="twitter:description" content="Test your predictions with virtual PRAE tokens. Auto-resolved by 46+ APIs.">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; connect-src 'self' https://akjxouwzsbewihvrdzyn.supabase.co wss://akjxouwzsbewihvrdzyn.supabase.co https://cdn.jsdelivr.net https://api.devnet.solana.com https://api.mainnet-beta.solana.com wss://api.devnet.solana.com; style-src 'self' 'unsafe-inline'; img-src data: https:; media-src 'self' data:; manifest-src data:;">
    <title>PRAEDICTA – For Those Who See</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><circle cx='50' cy='50' r='50' fill='%237C3AED'/><text x='50' y='67' text-anchor='middle' fill='%23F0F0F5' font-size='50' font-weight='bold'>P</text></svg>">
    <link rel="manifest" href="data:application/manifest+json,{%22name%22:%22PRAEDICTA%22,%22short_name%22:%22PRAEDICTA%22,%22start_url%22:%22.%22,%22display%22:%22standalone%22,%22background_color%22:%22%230A0A0F%22,%22theme_color%22:%22%237C3AED%22,%22icons%22:[{%22src%22:%22data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Ccircle cx='50' cy='50' r='50' fill='%237C3AED'/%3E%3Ctext x='50' y='67' text-anchor='middle' fill='%23F0F0F5' font-size='50' font-weight='bold'%3EP%3C/text%3E%3C/svg%3E%22,%22sizes%22:%22any%22}]}">

    <style>
        :root {
            --bg: #0A0A0F; --text: #F0F0F5; --accent: #7C3AED;
            --accent-glow: rgba(124, 58, 237, 0.3); --border: rgba(124, 58, 237, 0.1);
            --card-bg: rgba(124, 58, 237, 0.03); --card-border: rgba(124, 58, 237, 0.1);
            --text-muted: #8A8A9E; --text-secondary: #ccc;
            --stat-bg: rgba(124, 58, 237, 0.05);
            --input-bg: #1a1a2e; --input-border: #2a2a4a;
            --oracle-color: #F59E0B;
            --success-color: #10B981; --error-color: #EF4444; --info-color: #7C3AED;
            --badge-resolved: #A78BFA;
            --select-option-bg: #0A0A0F;
        }
        body.light {
            --bg: #FFFFFF; --text: #1A1A2E; --accent: #7C3AED;
            --accent-glow: rgba(124, 58, 237, 0.2); --border: rgba(124, 58, 237, 0.2);
            --card-bg: rgba(124, 58, 237, 0.03); --card-border: rgba(124, 58, 237, 0.1);
            --text-muted: #6B7280; --text-secondary: #4B5563;
            --stat-bg: rgba(124, 58, 237, 0.05);
            --input-bg: #F9FAFB; --input-border: #D1D5DB;
            --oracle-color: #F59E0B;
            --success-color: #059669; --error-color: #DC2626;
            --badge-resolved: #7C3AED;
            --select-option-bg: #FFFFFF;
        }
        *, *::before, *::after { margin:0; padding:0; box-sizing:border-box; transition: background-color .4s ease, color .4s ease, border-color .4s ease; }
        body { background: var(--bg); color: var(--text); font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; overflow-x: hidden; font-size: 16px; letter-spacing: -0.01em; line-height: 1.6; }
        h1, h2, h3 { letter-spacing: -0.02em; font-weight: 600; }
        ::-webkit-scrollbar { width: 6px; } ::-webkit-scrollbar-track { background: transparent; } ::-webkit-scrollbar-thumb { background: var(--accent-glow); border-radius: 3px; }

        .btn { padding:12px 28px; border-radius:50px; font-size:.8rem; font-weight:500; letter-spacing:2px; text-transform:uppercase; background:var(--accent); color:var(--bg); border:none; cursor:pointer; display:inline-flex; align-items:center; justify-content:center; gap:8px; transition: transform 0.15s ease; }
        .btn:active { transform: scale(0.95); } .btn:disabled { opacity:.5; cursor:not-allowed; }
        .card { background:var(--card-bg); border:1px solid var(--card-border); border-radius:24px; padding:20px; margin-bottom:20px; }
        .card:hover { border-color:var(--accent-glow); }

        .gate { position:fixed; top:0; left:0; right:0; bottom:0; display:flex; align-items:center; justify-content:center; z-index:1000; background:var(--bg); }
        .gate-card { background:var(--card-bg); border:1px solid var(--accent-glow); border-radius:48px; padding:40px; max-width:450px; width:90%; text-align:center; backdrop-filter:blur(4px); }
        .eye-container { width:100px; height:64px; margin:0 auto 24px; }
        .eye { width:100%; height:100%; border:2px solid var(--accent); border-radius:50%; position:relative; overflow:hidden; animation:blink 4s ease-in-out infinite; display:flex; align-items:center; justify-content:center; background:var(--bg); }
        .iris { width:45px; height:45px; background:radial-gradient(circle,#2D0B4F 0%,#6B2FA0 40%,var(--accent) 70%,var(--accent) 100%); border-radius:50%; animation:pulse 3s ease-in-out infinite; }
        .pupil { width:18px; height:18px; background:var(--bg); border-radius:50%; position:absolute; top:50%; left:50%; transform:translate(-50%,-50%); }
        .gate-card h2 { font-size:2rem; font-weight:300; letter-spacing:6px; color:var(--accent); margin-bottom:8px; }
        .gate-card .tagline { color:var(--text-muted); font-size:.8rem; letter-spacing:3px; margin-bottom:24px; }

        .main-app { display:none; max-width:1400px; margin:0 auto; padding:20px 24px; }
        .app-header { background: linear-gradient(135deg, var(--card-bg), var(--accent-glow)); border:1px solid var(--card-border); border-radius:24px; padding:20px; margin-bottom:30px; display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:20px; }
        .logo-area { display:flex; align-items:center; gap:16px; }
        .logo-eye { width:50px; height:32px; } .logo-eye .eye { width:100%; height:100%; border-width:1px; } .logo-eye .iris { width:22px; height:22px; } .logo-eye .pupil { width:8px; height:8px; }
        .logo-area h1 { font-size:1.8rem; font-weight:300; letter-spacing:6px; color:var(--accent); }
        .logo-area .tagline { font-size:.7rem; letter-spacing:2px; color:var(--text-muted); }
        .stats { display:flex; gap:20px; align-items:center; flex-wrap:wrap; }
        .stat { background:var(--stat-bg); padding:8px 20px; border-radius:40px; text-align:center; }
        .stat-value { font-size:1.5rem; font-weight:600; color:var(--accent); font-variant-numeric: tabular-nums; }

        .tabs { display:flex; flex-wrap:wrap; gap:8px; margin-bottom:24px; align-items:center; justify-content:space-between; }
        .tabs-left { display:flex; gap:8px; flex-wrap:wrap; }
        .tab-btn { background:transparent; border:1px solid var(--accent-glow); padding:8px 20px; border-radius:40px; cursor:pointer; color:var(--text-muted); font-size:.8rem; letter-spacing:1px; transition: all 0.2s ease; position: relative; }
        .tab-btn.active { background:var(--accent); color:var(--bg); border-color:var(--accent); box-shadow: 0 0 20px var(--accent-glow); }
        .tab-btn.active::after { content: ''; position: absolute; bottom: -4px; left: 25%; width: 50%; height: 2px; background: var(--accent); border-radius: 1px; }
        .tab-btn:active { transform: scale(0.95); }
        .tab-btn:hover:not(.active) { background:var(--accent-glow); color:var(--accent); }
        .right-tab-items { display:flex; gap:8px; align-items:center; flex-wrap:wrap; }
        .icon-btn { background:transparent; border:1px solid var(--accent); border-radius:40px; padding:8px 16px; cursor:pointer; color:var(--accent); font-size:.8rem; text-decoration:none; display:inline-flex; align-items:center; gap:6px; transition: all 0.2s ease; }
        .icon-btn:hover { background:var(--accent-glow); } .icon-btn:active { transform: scale(0.95); }
        .oracle-indicator-top { background:rgba(245,158,11,0.2); border:1px solid var(--oracle-color); border-radius:40px; padding:6px 14px; font-size:.7rem; color:var(--oracle-color); display:inline-flex; align-items:center; gap:6px; }
        .tab-content { display:none; } .tab-content.active { display:block; }

        .filter-bar { display:flex; gap:12px; flex-wrap:wrap; margin-top:16px; margin-bottom:24px; align-items:center; }
        .filter-bar select, .filter-bar input, input[type="text"], textarea, select { background:var(--input-bg); border:1px solid var(--input-border); border-radius:40px; padding:8px 16px; color:var(--text); font-size:.8rem; transition: border-color 0.2s ease, box-shadow 0.2s ease; }
        input:focus, textarea:focus, select:focus { outline: none; border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-glow); }
        .filter-bar select option, select option { background:var(--select-option-bg); color:var(--text); }
        .status-filter-btns { display:flex; gap:8px; }
        .status-filter-btn { background:transparent; border:1px solid var(--accent-glow); padding:8px 16px; border-radius:40px; cursor:pointer; color:var(--text-muted); font-size:.75rem; transition: all 0.2s ease; }
        .status-filter-btn.active-filter { background:var(--accent-glow); color:var(--accent); border-color:var(--accent); }
        .status-filter-btn:active { transform: scale(0.95); }
        .category-carousel { display:flex; gap:8px; overflow-x:auto; padding:8px 0; margin-bottom:16px; }
        .category-carousel-btn { background:transparent; border:1px solid var(--accent-glow); padding:8px 16px; border-radius:40px; cursor:pointer; color:var(--text-muted); font-size:.75rem; white-space:nowrap; transition: all 0.2s ease; }
        .category-carousel-btn.active-filter { background:var(--accent-glow); color:var(--accent); border-color:var(--accent); }
        .category-carousel-btn:active { transform: scale(0.95); }
        .category-carousel-btn:hover { background:var(--accent-glow); color:var(--accent); }

        .praedictions-grid { display:grid; grid-template-columns:repeat(auto-fill,minmax(360px,1fr)); gap:20px; margin-bottom:30px; }
        .praediction-card { background:var(--card-bg); border:1px solid var(--card-border); border-radius:24px; padding:24px; position:relative; transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1), padding 0.3s ease, transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease, border-left-width 0.3s ease !important; }
        .praediction-card:hover { transform: translateY(-4px); box-shadow: 0 12px 40px rgba(0,0,0,0.3); border-color: var(--accent); }
        .praediction-card.status-active { border-left: 3px solid var(--accent); }
        .praediction-card.status-expired { border-left: 3px solid var(--oracle-color); }
        .praediction-card.status-resolved { border-left: 3px solid var(--success-color); }
        .praediction-card.status-unresolvable { border-left: 3px solid var(--error-color); }
        .praediction-card.compact-mode { max-height: 200px; overflow: hidden; }
        .praediction-card.expanded-mode { max-height: 2000px; }
        .praediction-title { font-size:1.2rem; font-weight:500; color:var(--accent); margin-bottom:8px; word-break:break-word; }
        .praediction-desc { font-size:.9rem; margin-bottom:12px; color:var(--text-secondary); word-break:break-word; }
        .meta-row { display:flex; flex-wrap:wrap; justify-content:space-between; align-items:center; gap:8px; font-size:.7rem; color:var(--text-muted); border-top:1px solid var(--accent-glow); padding-top:8px; margin-bottom:12px; }

        .badge { display:inline-block; padding:2px 8px; border-radius:20px; font-size:.7rem; font-weight:500; transition: filter 0.2s ease; } .badge:hover { filter: brightness(1.2); }
        .badge-active { background:rgba(124,58,237,0.2); color:var(--accent); border:1px solid var(--accent); }
        .badge-resolved { background:rgba(167,139,250,0.2); color:var(--badge-resolved); border:1px solid var(--badge-resolved); }
        .badge-expired { background:rgba(255,68,68,0.2); color:#FF8888; border:1px solid #FF8888; }
        .badge-controversy { background:rgba(236,72,153,0.2); color:#EC4899; border:1px solid #EC4899; }
        .badge-flash { background:rgba(239,68,68,0.2); color:#EF4444; border:1px solid #EF4444; }
        .oracle-badge { background:rgba(245,158,11,0.2); color:var(--oracle-color); border:1px solid var(--oracle-color); }

        .vote-stats { display:flex; justify-content:space-between; background:var(--accent-glow); border-radius:40px; padding:8px 16px; font-size:.7rem; font-weight:500; }
        .action-buttons { display:flex; gap:12px; margin-top:16px; flex-wrap:wrap; }
        .btn-praedict, .btn-suggest { padding:8px 16px; border-radius:40px; font-weight:500; font-size:.8rem; cursor:pointer; flex:1; text-align:center; display:inline-flex; align-items:center; justify-content:center; gap:6px; transition: transform 0.15s ease; }
        .btn-praedict { background:var(--accent); color:var(--bg); border:none; } .btn-praedict:active { transform: scale(0.95); } .btn-praedict:disabled { opacity:.5; }
        .btn-suggest { background:rgba(124,58,237,0.2); color:var(--accent); border:1px solid var(--accent); } .btn-suggest:active { transform: scale(0.95); }
        .oracle-resolve { background:var(--oracle-color); color:#FFF; border:none; padding:8px; border-radius:40px; font-weight:600; cursor:pointer; margin-top:8px; width:100%; transition: transform 0.15s ease; } .oracle-resolve:active { transform: scale(0.95); }
        .loader { display:inline-block; width:14px; height:14px; border:2px solid rgba(255,255,255,0.3); border-radius:50%; border-top-color:currentColor; animation:spin .6s linear infinite; }

        .create-praediction input, .create-praediction textarea, .create-praediction select { width:100%; padding:10px; margin-bottom:12px; background:var(--input-bg); border:1px solid var(--input-border); border-radius:12px; color:var(--text); font-family:inherit; }
        .social-btn { background:rgba(124,58,237,0.1); color:var(--accent); padding:6px 12px; border-radius:40px; text-decoration:none; font-size:.8rem; border:1px solid var(--accent-glow); display:inline-block; transition: all 0.2s ease; } .social-btn:hover { background:var(--accent-glow); }

        .toast { position:fixed; bottom:20px; right:20px; padding:12px 24px; border-radius:40px; font-weight:bold; z-index:2000 !important; animation:toastBounce 0.5s ease-out !important; }
        .toast-success { background:var(--success-color); color:#000; } .toast-error { background:var(--error-color); color:#FFF; } .toast-info { background:var(--accent); color:var(--bg); }
        .toast button { white-space:nowrap; }
        .toast button:hover { background:rgba(255,255,255,0.3) !important; }

        .skeleton { background:linear-gradient(90deg,var(--card-bg) 25%,var(--accent-glow) 50%,var(--card-bg) 75%); background-size:200% 100%; animation:shimmer 1.5s infinite; border-radius:12px; } .skeleton-card { height:200px; margin-bottom:20px; }
        .resolution-info { margin-top:12px; padding:12px; background:var(--accent-glow); border-radius:12px; font-size:.75rem; } .resolution-info-grid { display:grid; grid-template-columns:auto 1fr; gap:4px 8px; }
        .offline-banner { display:none; background:var(--error-color); color:#FFF; text-align:center; padding:8px; border-radius:12px; margin-bottom:16px; cursor:pointer; }
        .empty-state { text-align:center; padding:40px 20px; color:var(--text-muted); } .empty-state-icon { font-size:3rem; margin-bottom:12px; } .empty-state p { margin-top:8px; line-height:1.6; }
        .price-highlight { background: linear-gradient(135deg, var(--accent), var(--oracle-color)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; font-weight: 700; font-variant-numeric: tabular-nums; }
        #creatorBetYes.selected { box-shadow: 0 0 15px rgba(124,58,237,0.6); border: 2px solid var(--accent); }
        #creatorBetNo.selected { box-shadow: 0 0 15px rgba(239,68,68,0.6); border: 2px solid #EF4444; }
        #particlesCanvas { position:fixed; top:0; left:0; z-index:-1; pointer-events:none; opacity:0.6; } body.light #particlesCanvas { opacity:0.35; }
        .user-profile-link { cursor:pointer; } .user-profile-link:hover { color: var(--oracle-color) !important; text-decoration: underline; }
        .filter-tag { font-size:.6rem;padding:2px 8px;border-radius:12px;background:var(--card-bg);border:1px solid var(--border);cursor:pointer;color:var(--text-muted);transition: all 0.2s ease; }
        .filter-tag.active-filter { background:var(--accent-glow);color:var(--accent);border-color:var(--accent); }
        .filter-tag:hover { background:var(--accent-glow);color:var(--accent); }

        #liveCounter { z-index:99; }
        .order-book-container { animation: slideDown 0.3s ease-out; }
        .stat-value { transition: transform 0.3s ease; } .stat-value:hover { transform: scale(1.1); }
        .number-animate { transition: color 0.3s ease; } .number-up { color: var(--success-color); } .number-down { color: var(--error-color); }

        /* Profile sections */
        .profile-section { border:1px solid var(--border);border-radius:16px;padding:16px;margin-bottom:12px;background:var(--card-bg);cursor:pointer;transition:border-color 0.3s ease; }
        .profile-section:hover { border-color:var(--accent); }
        .profile-section-header { display:flex;justify-content:space-between;align-items:center;color:var(--text);font-size:.9rem; }
        .profile-section-content { max-height:0;overflow:hidden;transition:max-height 0.4s ease; }
        .profile-section-content.open { max-height:2000px;margin-top:12px; }

        /* Copy trade button */
        .copy-trade-btn { opacity:0;transition:opacity 0.2s ease; }
        .bet-row:hover .copy-trade-btn { opacity:1; }

        button:focus-visible, input:focus-visible, select:focus-visible, textarea:focus-visible { outline:2px solid var(--accent);outline-offset:2px; }

        @keyframes fadeIn { from{opacity:0;transform:translateX(100%)} to{opacity:1;transform:translateX(0)} }
        @keyframes toastBounce { 0%{transform:translateX(100px);opacity:0} 60%{transform:translateX(-10px);opacity:1} 100%{transform:translateX(0);opacity:1} }
        @keyframes slideDown { from{opacity:0;max-height:0} to{opacity:1;max-height:500px} }
        @keyframes blink { 0%,90%,100%{transform:scaleY(1)} 95%{transform:scaleY(.1)} }
        @keyframes pulse { 0%,100%{opacity:.6} 50%{opacity:1} }
        @keyframes spin { to{transform:rotate(360deg)} }
        @keyframes shimmer { 0%{background-position:-200% 0} 100%{background-position:200% 0} }
        @keyframes betPulse { 0%{box-shadow:0 0 0 0 var(--accent)} 50%{box-shadow:0 0 15px 5px var(--accent-glow)} 100%{box-shadow:0 0 0 0 var(--accent)} } .bet-pulse { animation: betPulse 0.6s ease-out; }
        @keyframes glow { from{background:var(--accent-glow)} to{background:transparent} } .leaderboard-glow { animation: glow 1.5s ease-out; }
        @keyframes floatUp { 0%{opacity:1;transform:translateY(0) scale(1)} 100%{opacity:0;transform:translateY(-80px) scale(1.5)} }
        @keyframes confettiFall { 0%{transform:translateY(0) rotate(0deg);opacity:1} 100%{transform:translateY(100vh) rotate(720deg);opacity:0} }
        @keyframes flashGreen { 0%{background:rgba(16,185,129,0.3)} 100%{background:transparent} }
        @keyframes flashRed { 0%{background:rgba(239,68,68,0.3)} 100%{background:transparent} }
        @keyframes ripple { to{transform:scale(4);opacity:0} }

        @media(max-width:768px) {
            .app-header{flex-direction:column;align-items:flex-start} .stats{width:100%;justify-content:space-between;margin-top:16px;gap:8px} .stat{padding:6px 12px} .stat-value{font-size:1.2rem}
            .tabs{flex-direction:column;align-items:stretch} .tabs-left{justify-content:space-between} .right-tab-items{justify-content:space-between}
            .praedictions-grid{grid-template-columns:1fr} .praediction-card{padding:16px} .gate-card{padding:24px}
            .action-buttons{flex-direction:column} .filter-bar{flex-direction:column;align-items:stretch}
            #liveCounter{display:none!important}
            .create-praediction-wrapper{position:relative}
            .create-praediction{max-height:0;overflow:hidden;transition:max-height 0.4s cubic-bezier(0.4,0,0.2,1),margin 0.3s ease,padding 0.3s ease,border 0.3s ease;margin-bottom:0;padding:0 20px;border:none;border-radius:24px}
            .create-praediction.expanded{max-height:900px;margin-bottom:20px;padding:20px;border:1px solid var(--card-border)}
            .create-fab{position:fixed;bottom:20px;right:20px;width:56px;height:56px;border-radius:50%;background:var(--accent);color:var(--bg);border:none;font-size:1.5rem;cursor:pointer;z-index:100;box-shadow:0 4px 20px rgba(124,58,237,0.4);display:flex;align-items:center;justify-content:center;transition:transform 0.3s ease}
            .create-fab:active{transform:scale(0.9)}
            .create-fab.active{transform:rotate(45deg)}
        }
        @media(min-width:769px) {
            .create-fab{display:none!important}
            .create-praediction{max-height:none!important;overflow:visible!important}
        }
    </style>
</head>
<body>

<canvas id="particlesCanvas"></canvas>
<button id="backToTop" onclick="window.scrollTo({top:0,behavior:'smooth'})" style="position:fixed;bottom:20px;left:20px;width:40px;height:40px;border-radius:50%;background:var(--accent);color:var(--bg);border:none;cursor:pointer;z-index:100;display:none;font-size:1.2rem;">↑</button>

<div id="liveCounter" style="position:fixed;bottom:80px;left:20px;background:var(--accent);color:var(--bg);padding:8px 16px;border-radius:20px;font-size:.7rem;z-index:99;display:none;box-shadow:0 4px 20px rgba(124,58,237,0.3);"></div>

<div id="notifCenter" style="display:none;position:fixed;top:80px;right:20px;width:360px;max-width:90vw;max-height:70vh;background:var(--bg);border:2px solid var(--accent);border-radius:20px;z-index:4000;overflow-y:auto;box-shadow:0 20px 60px rgba(0,0,0,0.5);">
    <div style="padding:16px;position:sticky;top:0;background:var(--bg);border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center;">
        <h3 style="color:var(--accent);">🔔 Notifications</h3>
        <button onclick="clearAllNotifs()" style="background:transparent;border:none;color:var(--text-muted);cursor:pointer;font-size:.7rem;">Clear all</button>
    </div>
    <div id="notifList" style="padding:8px 16px;"></div>
</div>

<div id="tutorialOverlay" style="display:none;">
    <div class="tutorial-card overlay-message" style="position:static;transform:none;">
        <div class="eye-container" style="width:80px;height:50px;margin:0 auto 16px;"><div class="eye"><div class="iris"><div class="pupil"></div></div></div></div>
        <h2 style="color:var(--accent);">Welcome to P R A E D I C T A</h2>
        <p style="margin:16px 0;line-height:1.6;">✨ Create praedictions about future events<br>💰 Commit YES or NO with virtual PRAE tokens<br>🤖 46+ APIs auto-resolve outcomes<br>📎 Source URL required for manual resolution</p>
        <button id="closeTutorialBtn" class="btn">Let's go!</button>
    </div>
</div>

<div id="gate" class="gate">
    <div class="gate-card">
        <div class="eye-container"><div class="eye"><div class="iris"><div class="pupil"></div></div></div></div>
        <h2>PRAEDICTA</h2><div class="tagline">for those who see</div>
        <div class="age-consent" style="margin:16px 0;">
            <input type="checkbox" id="ageCheckbox" aria-label="Age confirmation">
            <label for="ageCheckbox">I am 18+ and understand this is a <strong>free prediction game</strong>. Virtual tokens only. <a href="#" class="legal-link" data-doc="terms" style="color:var(--accent);">Game Rules</a></label>
        </div>
        <input type="email" id="emailInput" placeholder="Email for notifications (optional)" style="width:100%; padding:12px; background:var(--input-bg); border:1px solid var(--input-border); border-radius:40px; color:var(--text); text-align:center; font-size:.8rem; margin-bottom:12px;" autocomplete="email">
        <button id="connectBtn" class="btn"><span>Connect Wallet</span><span class="loader" style="display:none;"></span></button>
        <p style="color:var(--text-muted); font-size:.7rem; margin-top:8px;">🔐 Free to play – no real money involved</p>
        <p style="color:var(--text-muted); font-size:.65rem; margin-top:4px;">Virtual tokens for entertainment only · No cash value · Cannot be withdrawn</p>
        <div id="gateMessage" style="margin-top:20px; color:#8A8A8A;"></div>
        <div id="emailVerifySection" style="display:none; margin-top:12px; padding:12px; background:var(--accent-glow); border-radius:12px;">
            <p style="font-size:.8rem;color:var(--accent);">📧 Verify your email for notifications</p>
            <div style="display:flex;gap:8px;margin-top:8px;">
                <input type="text" id="emailVerifyCode" placeholder="Enter 6-digit code" maxlength="6" style="flex:1; text-align:center; letter-spacing:4px; background:var(--input-bg); border:1px solid var(--input-border); border-radius:40px; padding:8px 16px; color:var(--text);">
                <button id="verifyEmailBtn" class="btn" style="font-size:.7rem;">Verify</button>
            </div>
            <button id="sendVerifyEmailBtn" class="btn" style="width:100%;margin-top:8px;font-size:.7rem;background:var(--card-bg);color:var(--accent);border:1px solid var(--accent);">Send Verification Code</button>
        </div>
    </div>
</div>

<div id="mainApp" class="main-app">
    <header class="app-header" role="banner">
        <div class="logo-area">
            <div class="logo-eye" id="logoEyeWrapper" aria-hidden="true"><div class="eye"><div class="iris"><div class="pupil"></div></div></div></div>
            <div><h1>PRAEDICTA</h1><div class="tagline">for those who see</div></div>
        </div>
        <div class="stats" role="status" aria-live="polite">
            <div class="stat"><div class="stat-value" id="totalActive">0</div><div>Active</div></div>
            <div class="stat"><div class="stat-value" id="totalPraedicts">0</div><div>PRAE Staked</div></div>
            <div class="stat"><div class="stat-value" id="totalVolume">0</div><div>📊 Vol</div></div>
            <div class="stat"><div class="stat-value" id="totalPredictions">0</div><div>Total</div></div>
            <div class="stat"><div class="stat-value" id="totalUsers">0</div><div>👥 Seers</div></div>
            <div class="stat"><div class="stat-value" id="totalSeerscore">0</div><div>👁️ Score</div></div>
            <div class="stat" id="notificationBadge" style="display:none; cursor:pointer;" onclick="this.style.display='none';"><div class="stat-value" style="font-size:1rem;" id="notificationText">🔔</div></div>
        </div>
    </header>

    <div id="offlineBanner" class="offline-banner" onclick="refreshAll()">⚠️ Connection lost. Tap to retry.</div>

    <nav class="tabs" role="tablist">
        <div class="tabs-left">
            <button class="tab-btn active" data-tab="praedictions">🌌 Praedictions</button>
            <button class="tab-btn" data-tab="profile">👁️ Profile</button>
            <button class="tab-btn" data-tab="leaderboard">🏆 Leaderboard</button>
            <button class="tab-btn" data-tab="support">💛 Support</button>
            <button class="tab-btn" data-tab="admin" id="adminTabBtn" style="display:none;">🦉 Admin</button>
        </div>
        <div class="right-tab-items">
            <a href="https://t.me/praedictacoin" target="_blank" rel="noopener" class="icon-btn">📱</a>
            <a href="https://dexscreener.com/solana/7C2Y5NebLFG37wMbB85TbMqYERTSkq9tEi58wv28MCzt" target="_blank" rel="noopener" class="icon-btn">👁️ PRAE</a>
            <button id="oracleBtn" class="icon-btn" title="Ask the Oracle">🔮</button>
            <button id="notifCenterBtn" class="icon-btn" style="position:relative;" onclick="toggleNotifCenter()">🔔<span id="notifBadge" style="position:absolute;top:-4px;right:-4px;background:var(--error-color);color:#FFF;border-radius:50%;width:18px;height:18px;font-size:.6rem;display:none;align-items:center;justify-content:center;">0</span></button>
            <button id="playToggle" class="icon-btn">🎮 Play</button>
            <button id="themeToggleSmall" class="icon-btn">🌓</button>
            <button id="disconnectBtn" class="icon-btn" style="display:none;">🔌</button>
            <div id="oracleIndicatorTop" class="oracle-indicator-top" style="display:none;">🦉 ORACLE</div>
        </div>
    </nav>

    <section id="tab-praedictions" class="tab-content active" role="tabpanel">
        <div id="quickStartWidget"></div>
        <div id="voiceMessage" style="text-align:center;padding:8px;color:var(--accent);font-style:italic;"></div>
        <div class="category-carousel" id="categoryCarousel">
            <button class="category-carousel-btn active-filter" data-category="all">All</button>
            <button class="category-carousel-btn" data-category="finance">💰 Finance</button>
            <button class="category-carousel-btn" data-category="politics">🏛️ Politics</button>
            <button class="category-carousel-btn" data-category="sports">⚽ Sports</button>
            <button class="category-carousel-btn" data-category="weather">🌤️ Weather</button>
            <button class="category-carousel-btn" data-category="entertainment">🎬 Entertainment</button>
            <button class="category-carousel-btn" data-category="technology">🔧 Tech</button>
            <button class="category-carousel-btn" data-category="space">🚀 Space</button>
            <button class="category-carousel-btn" data-category="wildcard">🎲 Wildcard</button>
        </div>
        <div class="filter-bar">
            <div class="status-filter-btns">
                <button class="status-filter-btn active-filter" data-status="active">Active</button>
                <button class="status-filter-btn" data-status="expired">Expired</button>
                <button class="status-filter-btn" data-status="resolved">Resolved</button>
                <button class="status-filter-btn" data-status="ending-soon">⏰ Ending</button>
            </div>
            <select id="filterCategory" aria-label="Filter">
                <option value="all">All</option><option value="finance">💰 Finance</option><option value="politics">🏛️ Politics</option>
                <option value="sports">⚽ Sports</option><option value="weather">🌤️ Weather</option><option value="entertainment">🎬 Entertainment</option>
                <option value="technology">🔧 Tech</option><option value="space">🚀 Space</option><option value="wildcard">🎲 Wildcard</option>
            </select>
            <input type="text" id="filterSearch" placeholder="Search..." inputmode="search">
            <button id="revealVotesBtn" class="status-filter-btn">👁️ Hide Votes</button>
        </div>
        <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px;align-items:center;">
            <select id="filterSortBy" onchange="applyAdvancedFilters()" style="font-size:.7rem;background:var(--input-bg);border:1px solid var(--input-border);border-radius:20px;padding:4px 12px;color:var(--text);">
                <option value="newest">🆕 Newest</option><option value="oldest">📅 Oldest</option><option value="volume">💰 Highest Volume</option><option value="controversial">⚡ Most Controversial</option><option value="ending-soon">⏰ Ending Soon</option><option value="bettors">👥 Most Bettors</option>
            </select>
            <input type="number" id="filterMinVolume" placeholder="Min PRAE" onchange="applyAdvancedFilters()" style="width:80px;font-size:.7rem;background:var(--input-bg);border:1px solid var(--input-border);border-radius:20px;padding:4px 8px;color:var(--text);">
            <span style="color:var(--text-muted);font-size:.7rem;">–</span>
            <input type="number" id="filterMaxVolume" placeholder="Max PRAE" onchange="applyAdvancedFilters()" style="width:80px;font-size:.7rem;background:var(--input-bg);border:1px solid var(--input-border);border-radius:20px;padding:4px 8px;color:var(--text);">
            <span class="filter-tag" data-tag="crypto" onclick="this.classList.toggle('active-filter');applyAdvancedFilters()">🪙 Crypto</span>
            <span class="filter-tag" data-tag="stocks" onclick="this.classList.toggle('active-filter');applyAdvancedFilters()">📈 Stocks</span>
            <span class="filter-tag" data-tag="weather" onclick="this.classList.toggle('active-filter');applyAdvancedFilters()">🌤️ Weather</span>
            <span class="filter-tag" data-tag="sports" onclick="this.classList.toggle('active-filter');applyAdvancedFilters()">⚽ Sports</span>
        </div>
        <div id="hottestCategory" style="text-align:center;padding:8px;color:var(--accent);font-size:.85rem;"></div>
        <div id="dailyChallenge"></div>
        <div id="progressBars" style="margin-bottom:8px;"></div>
        <div id="activityFeed" style="margin-bottom:16px; padding:12px; background:var(--card-bg); border-radius:12px; border:1px solid var(--card-border); max-height:200px; overflow-y:auto;"></div>
        <div id="recentWinners" style="margin-bottom:16px; padding:12px; background:var(--card-bg); border-radius:12px; border:1px solid var(--card-border);"></div>
        <div id="skeletonContainer" class="praedictions-grid"><div class="skeleton skeleton-card"></div><div class="skeleton skeleton-card"></div><div class="skeleton skeleton-card"></div></div>
        <div id="praedictionsContainer" class="praedictions-grid" role="list" style="display:none;"></div>
        <div id="expiredContainer" style="margin-top:30px;"></div>
        <div id="resolvedContainer" style="margin-top:30px;"></div>
        <button id="loadMoreBtn" class="btn" style="width:100%; margin-top:16px; background:var(--card-bg); color:var(--accent); border:1px solid var(--accent); display:none;">Load More</button>
        
        <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px;">
            <button onclick="quickTemplate('btc')" style="font-size:.65rem;padding:4px 10px;border-radius:20px;background:var(--accent-glow);border:1px solid var(--accent);color:var(--accent);cursor:pointer;">📈 BTC</button>
            <button onclick="quickTemplate('eth')" style="font-size:.65rem;padding:4px 10px;border-radius:20px;background:var(--accent-glow);border:1px solid var(--accent);color:var(--accent);cursor:pointer;">📈 ETH</button>
            <button onclick="quickTemplate('weather')" style="font-size:.65rem;padding:4px 10px;border-radius:20px;background:var(--accent-glow);border:1px solid var(--accent);color:var(--accent);cursor:pointer;">🌤️ Weather</button>
            <button onclick="quickTemplate('sports')" style="font-size:.65rem;padding:4px 10px;border-radius:20px;background:var(--accent-glow);border:1px solid var(--accent);color:var(--accent);cursor:pointer;">⚽ Sports</button>
            <button onclick="quickTemplate('movie')" style="font-size:.65rem;padding:4px 10px;border-radius:20px;background:var(--accent-glow);border:1px solid var(--accent);color:var(--accent);cursor:pointer;">🎬 Movie</button>
            <button onclick="quickTemplate('spacex')" style="font-size:.65rem;padding:4px 10px;border-radius:20px;background:var(--accent-glow);border:1px solid var(--accent);color:var(--accent);cursor:pointer;">🚀 SpaceX</button>
        </div>
        <button id="quickWeather" class="status-filter-btn" style="margin-bottom:8px;">🌤️ Quick Weather Praediction</button>

        <div class="create-praediction-wrapper">
            <div class="create-praediction card">
                <h3 style="margin-bottom:12px;">I praedict that…</h3>
                <input type="text" id="title" placeholder="BTC will reach $120,000 by July 2026" maxlength="200" autocomplete="off">
                <textarea id="description" rows="2" placeholder="With ETF inflows and halving effects..." maxlength="1000" autocomplete="off"></textarea>
                <select id="category" aria-label="Category">
                    <option value="">Select category</option><option value="finance">💰 Finance</option><option value="politics">🏛️ Politics</option><option value="sports">⚽ Sports</option><option value="weather">🌤️ Weather</option><option value="entertainment">🎬 Entertainment</option><option value="technology">🔧 Technology</option><option value="health">🦠 Health</option><option value="space">🚀 Space</option><option value="wildcard">🎲 Wildcard</option>
                </select>
                <input type="datetime-local" id="resolutionDate" aria-label="Resolution date">
                <select id="autoSource" aria-label="Auto-resolve source">
                    <option value="">No auto‑resolve (Oracle resolves)</option>
                    <optgroup label="📊 Crypto"><option value="coingecko_btc">BTC (CoinGecko)</option><option value="coingecko_eth">ETH</option><option value="coingecko_sol">SOL</option><option value="redstone_btc">BTC (RedStone)</option><option value="binance_btc">BTC (Binance)</option></optgroup>
                    <optgroup label="📈 Stocks"><option value="redstone_tsla">Tesla</option><option value="redstone_aapl">Apple</option><option value="redstone_nvda">Nvidia</option><option value="redstone_gold">Gold</option></optgroup>
                    <optgroup label="🌤️ Weather"><option value="weather_temp">Temperature (°C)</option><option value="weather_rain">Rain (mm)</option><option value="weather_wind">Wind (km/h)</option></optgroup>
                    <optgroup label="⚽ Sports"><option value="sports_">Team Win</option></optgroup>
                    <optgroup label="🎬 Movies"><option value="movie_">Box Office</option></optgroup>
                    <optgroup label="🌍 Other"><option value="wiki_">Wikipedia Event</option><option value="news_">News Headline</option><option value="quake_">Earthquake</option><option value="spacex_">SpaceX Launch</option><option value="fed_rate">Fed Rate</option></optgroup>
                </select>
                <input type="text" id="autoSourceDetail" placeholder="City, team, or page name" maxlength="100" style="display:none; width:100%; padding:10px; margin-bottom:12px; background:var(--input-bg); border:1px solid var(--input-border); border-radius:12px; color:var(--text); font-family:inherit;">
                <input type="text" id="targetValue" placeholder="Target value (price, temperature, score)" maxlength="50" autocomplete="off">
                <input type="url" id="sourceUrl" placeholder="Proof URL (required if no auto-resolve)" maxlength="500" style="width:100%; padding:10px; margin-bottom:12px; background:var(--input-bg); border:1px solid var(--input-border); border-radius:12px; color:var(--text); font-family:inherit;">
                <div style="margin:12px 0; padding:12px; background:var(--accent-glow); border-radius:12px;">
                    <p style="color:var(--accent); font-weight:500; margin-bottom:8px;">Your conviction (required - 7 PRAE):</p>
                    <div style="display:flex; gap:8px;"><button id="creatorBetYes" class="btn-praedict" type="button" style="flex:1;">✅ Commit YES</button><button id="creatorBetNo" class="btn-praedict" type="button" style="flex:1; background:rgba(239,68,68,0.8);">❌ Commit NO</button></div>
                    <p id="creatorBetDisplay" style="text-align:center; margin-top:8px; font-size:.8rem; color:var(--text-muted);">Select your conviction</p>
                </div>
                <button id="createBtn" class="btn" style="width:100%;"><span>✨ Create & Stake 7 PRAE</span><span class="loader" style="display:none;"></span></button>
            </div>
        </div>
        <button class="create-fab" id="createFab" onclick="toggleCreateForm()" title="Create Prediction">✨</button>
    </section>

    <section id="tab-profile" class="tab-content" role="tabpanel">
        <div class="card">
            <h2 style="color:var(--accent); text-align:center; font-size:1.2rem;"><span id="walletDisplay"></span><span id="oracleIndicatorProfile" style="display:none;" class="oracle-badge">🦉 ORACLE</span></h2>
            <div style="margin-top:4px; font-size:.7rem; color:var(--text-muted);"><span id="profileWalletAddress"></span><button id="copyWalletBtn" class="social-btn" style="font-size:.65rem; padding:2px 8px; margin-left:4px;">📋</button></div>

            <div class="profile-section" onclick="toggleProfileSection(this)">
                <div class="profile-section-header"><span>📊 Stats & Progress</span><span class="section-toggle-icon">▼</span></div>
                <div class="profile-section-content open">
                    <div id="profileStats" style="margin-top:12px;"></div>
                    <div id="dailyDigest" style="background:var(--card-bg);border:1px solid var(--accent-glow);border-radius:16px;padding:12px;margin-top:8px;text-align:center;"></div>
                    <div id="streakRewards" style="margin-top:8px; padding:12px; background:var(--card-bg); border-radius:12px; text-align:center;">🔥 Streak: <strong id="streakCount">0</strong> days</div>
                    <div id="streakCalendar"></div>
                </div>
            </div>

            <div class="profile-section" onclick="toggleProfileSection(this)">
                <div class="profile-section-header"><span>💼 Portfolio</span><span class="section-toggle-icon">▶</span></div>
                <div class="profile-section-content">
                    <button id="showPortfolioBtn" class="btn" style="width:100%; margin-top:8px; background:var(--card-bg); color:var(--accent); border:1px solid var(--accent);">💼 View Portfolio</button>
                    <div id="portfolioView" style="display:none; margin-top:8px;"></div>
                    <button id="showRecapBtn" class="btn" style="width:100%; margin-top:8px; background:var(--card-bg); color:var(--accent); border:1px solid var(--accent);">📊 Weekly Recap</button>
                    <button id="exportAnalyticsBtn" class="btn" style="width:100%; margin-top:8px; background:var(--card-bg); color:var(--accent); border:1px solid var(--accent);">📊 Export Analytics</button>
                </div>
            </div>

            <div class="profile-section" onclick="toggleProfileSection(this)">
                <div class="profile-section-header"><span>👤 Profile Settings</span><span class="section-toggle-icon">▶</span></div>
                <div class="profile-section-content">
                    <div id="profileNameSection" style="margin-top:8px; display:flex; gap:12px; align-items:center;"><input type="text" id="displayNameInput" placeholder="Your Seer name" maxlength="20" style="flex:1;" autocomplete="name"><select id="avatarSelect" style="background:var(--input-bg); border:1px solid var(--input-border); border-radius:40px; padding:8px 16px; color:var(--text);"></select><button id="saveProfileBtn" class="social-btn">Save</button></div>
                    <div style="margin-top:8px; display:flex; gap:8px; align-items:center;"><input type="file" id="avatarFileInput" accept="image/png,image/jpeg,image/gif,image/webp" style="display:none;" onchange="handleAvatarUpload(event)"><button onclick="document.getElementById('avatarFileInput').click()" class="social-btn" style="font-size:.7rem;">📷 Upload Avatar</button><div id="avatarPreview" style="display:none; width:40px; height:40px; border-radius:50%; overflow:hidden; border:2px solid var(--accent);"><img id="avatarPreviewImg" src="" style="width:100%;height:100%;object-fit:cover;" alt="Avatar preview"></div></div>
                    <div id="zodiacSelectorWrapper" style="margin-top:8px;"><span style="color:var(--accent);">⭐ Zodiac:</span> <select id="zodiacSelect"></select> <button id="saveZodiacBtn" class="social-btn">Save</button></div>
                    <div id="userZodiacDisplay" style="display:none; text-align:center; font-size:2rem;"></div>
                    <div id="zodiacRealHeadline" style="display:none; color:var(--accent); text-align:center;">Your Daily Praediction:</div>
                    <div id="zodiacHoroscopeDisplay" style="margin-top:8px; padding:12px; background:var(--card-bg); border-radius:20px;"></div>
                    <div id="lunarPhase" style="text-align:center;font-size:1.2rem;margin-top:8px;"></div>
                    <div id="streakStory" style="font-size:.8rem;color:var(--accent);text-align:center;margin-top:8px;font-style:italic;"></div>
                    <div id="freezeTimer" style="font-size:.7rem;color:var(--accent);text-align:center;"></div>
                </div>
            </div>

            <div class="profile-section" onclick="toggleProfileSection(this)">
                <div class="profile-section-header"><span>⚡ Actions</span><span class="section-toggle-icon">▶</span></div>
                <div class="profile-section-content">
                    <button id="flipCoinBtn" class="btn" style="width:100%; margin-top:8px;">🪙 Flip Coin (1 available)</button>
                    <div class="referral-box" style="margin-top:12px;"><strong style="color:var(--accent);">🔗 Invite Friends</strong><br><span id="referralLink"></span><br><button id="copyReferralBtn" class="social-btn" style="margin-top:8px;">📋 Copy Link</button></div>
                    <button id="showMyPraedictionsBtn" class="btn" style="width:100%; margin-top:12px; background:var(--card-bg); color:var(--accent); border:1px solid var(--accent);">📋 My Praedictions</button>
                    <div id="myPraedictionsList" style="display:none; margin-top:8px;"></div>
                </div>
            </div>

            <div class="profile-section" onclick="toggleProfileSection(this)">
                <div class="profile-section-header"><span>🔔 Notifications</span><span class="section-toggle-icon">▶</span></div>
                <div class="profile-section-content">
                    <label style="display:flex;align-items:center;gap:8px;cursor:pointer;color:var(--text);margin-top:8px;"><input type="checkbox" id="notifResolved" checked onchange="saveNotificationPrefs()"> Resolved predictions</label>
                    <label style="display:flex;align-items:center;gap:8px;cursor:pointer;color:var(--text);"><input type="checkbox" id="notifWon" checked onchange="saveNotificationPrefs()"> Won predictions</label>
                    <label style="display:flex;align-items:center;gap:8px;cursor:pointer;color:var(--text);"><input type="checkbox" id="notifStreak" checked onchange="saveNotificationPrefs()"> Streak milestones</label>
                    <label style="display:flex;align-items:center;gap:8px;cursor:pointer;color:var(--text);"><input type="checkbox" id="notifLeaderboard" checked onchange="saveNotificationPrefs()"> Leaderboard changes</label>
                    <label style="display:flex;align-items:center;gap:8px;cursor:pointer;color:var(--text);"><input type="checkbox" id="notifSound" checked onchange="saveNotificationPrefs()"> Sound effects</label>
                    <label style="display:flex;align-items:center;gap:8px;cursor:pointer;color:var(--text);"><input type="checkbox" id="notifTwoFactor" onchange="saveNotificationPrefs()"> 🔐 Two-Factor Authentication</label>
                    <button id="send2FACodeBtn" class="social-btn" style="width:100%;margin-top:8px;font-size:.7rem;">📧 Send 2FA Code</button>
                </div>
            </div>

            <div style="text-align:center;padding:12px;margin-top:16px;background:var(--card-bg);border-radius:12px;">👁️ P R A E D I C T A<br><a href="https://dexscreener.com/solana/7C2Y5NebLFG37wMbB85TbMqYERTSkq9tEi58wv28MCzt" target="_blank" rel="noopener" style="color:var(--accent);">Get PRAE →</a></div>
            <div class="legal-banner" style="text-align:center;padding:16px;margin-top:16px;font-size:.75rem;color:var(--text-muted);">
                🎮 Free prediction game · Virtual tokens only · No real money · <a href="#" class="legal-link" data-doc="terms">Game Rules</a> | <a href="#" class="legal-link" data-doc="privacy">Privacy</a> | <a href="#" id="deleteAccountLink" style="color:#ff8888;">Clear Data</a><br>
                <button onclick="localStorage.removeItem('tour_completed');startGuidedTour()" style="background:transparent;border:none;color:var(--text-muted);cursor:pointer;font-size:.7rem;margin-top:8px;">🔄 Restart Tutorial</button>
            </div>
        </div>
    </section>

    <section id="tab-leaderboard" class="tab-content" role="tabpanel">
        <div id="hypeMessage" style="text-align:center;padding:8px;color:var(--accent);font-style:italic;"></div>
        <div class="card">
            <h3 style="color:var(--accent);">🏆 TOP SEERS</h3>
            <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px;"><button class="status-filter-btn active-filter" data-period="all">All Time</button><button class="status-filter-btn" data-period="month">This Month</button></div>
            <select id="leaderboardCategoryFilter" style="margin-bottom:12px; width:100%; background:var(--input-bg); border:1px solid var(--input-border); border-radius:40px; padding:8px 16px; color:var(--text);">
                <option value="">All Categories</option><option value="finance">💰 Finance</option><option value="politics">🏛️ Politics</option><option value="sports">⚽ Sports</option><option value="weather">🌤️ Weather</option><option value="entertainment">🎬 Entertainment</option><option value="technology">🔧 Tech</option><option value="space">🚀 Space</option><option value="wildcard">🎲 Wildcard</option>
            </select>
            <div id="leaderboardList"></div>
            <div id="seerOfTheDay" style="margin-top:16px;padding:12px;background:var(--accent-glow);border-radius:12px;text-align:center;"></div>
        </div>
        <div class="card" style="margin-top:20px;">
            <h3 style="color:var(--accent);text-align:center;">🏛️ Hall of Fame</h3>
            <p style="text-align:center;margin-bottom:16px;color:var(--text-muted);font-size:.8rem;">Most accurate resolved praedictions</p>
            <div id="hallOfFameList"></div>
        </div>
    </section>

    <section id="tab-support" class="tab-content" role="tabpanel">
        <div class="card">
            <h3 style="color:var(--accent);text-align:center;">💛 Support PRAEDICTA</h3>
            <p style="text-align:center;color:var(--text-muted);font-size:.8rem;">Help us keep the Oracle running. Add liquidity, spread the word.</p>
            <div style="display:flex;gap:12px;flex-wrap:wrap;justify-content:center;margin:16px 0;">
                <a href="https://t.me/praedictacoin" target="_blank" rel="noopener" class="social-btn">📱 Telegram</a>
                <a href="https://x.com/PraedictaCoin" target="_blank" rel="noopener" class="social-btn">🐦 X</a>
                <a href="https://dexscreener.com/solana/7C2Y5NebLFG37wMbB85TbMqYERTSkq9tEi58wv28MCzt" target="_blank" rel="noopener" class="social-btn">📊 DexScreener</a>
            </div>
            <button id="donateBtn" class="btn" style="width:100%;margin-top:20px;">Support 👁️ Add Liquidity</button>
        </div>
        <div class="card" style="margin-top:20px;">
            <h3 style="color:var(--accent);text-align:center;">🏆 Tournaments</h3>
            <p style="text-align:center;color:var(--text-muted);font-size:.75rem;margin-bottom:12px;">Compete with other Seers for glory and prizes!</p>
            <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:12px;">
                <div style="padding:16px;background:var(--card-bg);border:1px solid var(--accent-glow);border-radius:16px;text-align:center;"><div style="font-size:1.2rem;font-weight:600;color:var(--accent);">📅 Weekly Cup</div><div style="font-size:.8rem;color:var(--text-muted);line-height:1.6;">🎯 Most accurate<br>💰 500 PRAE pool</div><button onclick="joinTournament('weekly')" class="btn" style="width:100%;margin-top:12px;font-size:.7rem;">Join · 10 PRAE</button></div>
                <div style="padding:16px;background:var(--card-bg);border:1px solid var(--oracle-color);border-radius:16px;text-align:center;"><div style="font-size:1.2rem;font-weight:600;color:var(--oracle-color);">🏆 Monthly Championship</div><div style="font-size:.8rem;color:var(--text-muted);line-height:1.6;">🎯 Highest profit<br>💰 2,000 PRAE pool</div><button onclick="joinTournament('monthly')" class="btn" style="width:100%;margin-top:12px;font-size:.7rem;background:var(--oracle-color);">Join · 25 PRAE</button></div>
                <div style="padding:16px;background:var(--card-bg);border:1px solid var(--success-color);border-radius:16px;text-align:center;"><div style="font-size:1.2rem;font-weight:600;color:var(--success-color);">🎪 Special Event</div><div style="font-size:.8rem;color:var(--text-muted);line-height:1.6;">🎯 Sponsored<br>💰 Varies</div><button onclick="joinTournament('special')" class="btn" style="width:100%;margin-top:12px;font-size:.7rem;background:var(--success-color);">View Details</button></div>
            </div>
        </div>
        <div class="card" style="margin-top:20px;">
            <h3 style="color:var(--accent);text-align:center;">🤝 Sponsor a Tournament</h3>
            <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:12px;margin-top:12px;">
                <div style="padding:16px;background:var(--card-bg);border-radius:12px;text-align:center;"><div style="font-size:2rem;">🥉</div><h4 style="color:var(--accent);">Bronze</h4><div style="font-size:1.2rem;font-weight:700;">500 PRAE</div><button onclick="becomeSponsor('bronze')" class="btn" style="width:100%;margin-top:8px;font-size:.7rem;">Sponsor</button></div>
                <div style="padding:16px;background:var(--card-bg);border:1px solid var(--accent);border-radius:12px;text-align:center;"><div style="font-size:2rem;">🥈</div><h4 style="color:var(--accent);">Silver</h4><div style="font-size:1.2rem;font-weight:700;">2,000 PRAE</div><button onclick="becomeSponsor('silver')" class="btn" style="width:100%;margin-top:8px;font-size:.7rem;">Sponsor</button></div>
                <div style="padding:16px;background:var(--card-bg);border:1px solid var(--oracle-color);border-radius:12px;text-align:center;"><div style="font-size:2rem;">🥇</div><h4 style="color:var(--oracle-color);">Gold</h4><div style="font-size:1.2rem;font-weight:700;">10,000 PRAE</div><button onclick="becomeSponsor('gold')" class="btn" style="width:100%;margin-top:8px;font-size:.7rem;background:var(--oracle-color);">Sponsor</button></div>
            </div>
        </div>
        <div class="card" style="margin-top:20px;"><h3>❓ FAQ</h3><div id="faqContainer"></div></div>
    </section>

    <section id="tab-admin" class="tab-content" role="tabpanel">
        <div id="adminDashboard"></div>
    </section>
</div>

<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="js/config.js"></script><script src="js/state.js"></script><script src="js/dom.js"></script>
<script src="js/utils.js"></script><script src="js/market.js"></script><script src="js/render.js"></script>
<script src="js/profile.js"></script><script src="js/auth.js"></script><script src="js/events.js"></script><script src="js/admin.js"></script><script src="js/init.js"></script>

<script>
(function(){const canvas=document.getElementById('particlesCanvas');if(!canvas)return;const ctx=canvas.getContext('2d');let particles=[];const maxParticles=40;function resize(){canvas.width=window.innerWidth;canvas.height=window.innerHeight}window.addEventListener('resize',resize);resize();class Particle{constructor(){this.reset()}reset(){this.x=Math.random()*canvas.width;this.y=canvas.height+Math.random()*100;this.size=Math.random()*3+1;this.speed=Math.random()*0.5+0.2;this.opacity=Math.random()*0.5+0.3;this.wobble=Math.random()*Math.PI*2}update(){this.y-=this.speed;this.wobble+=0.01;this.x+=Math.sin(this.wobble)*0.3;if(this.y<-10)this.reset()}draw(){const accent=getComputedStyle(document.body).getPropertyValue('--accent').trim()||'#7C3AED';ctx.fillStyle=accent;ctx.globalAlpha=this.opacity;ctx.beginPath();ctx.arc(this.x,this.y,this.size,0,Math.PI*2);ctx.fill()}}for(let i=0;i<maxParticles;i++)particles.push(new Particle());function animate(){ctx.clearRect(0,0,canvas.width,canvas.height);particles.forEach(p=>{p.update();p.draw()});ctx.globalAlpha=1;requestAnimationFrame(animate)}animate()})();
</script>
<script>
const sounds={flip:()=>{try{const a=new AudioContext();const o=a.createOscillator();const g=a.createGain();o.connect(g);g.connect(a.destination);o.frequency.value=800;g.gain.value=0.1;o.start();g.gain.exponentialRampToValueAtTime(0.001,a.currentTime+0.3);o.stop(a.currentTime+0.3)}catch(e){}},win:()=>{try{const a=new AudioContext();[523,659,784].forEach((f,i)=>{const o=a.createOscillator();const g=a.createGain();o.connect(g);g.connect(a.destination);o.frequency.value=f;g.gain.value=0.1;o.start(a.currentTime+i*0.15);g.gain.exponentialRampToValueAtTime(0.001,a.currentTime+i*0.15+0.4);o.stop(a.currentTime+i*0.15+0.4)})}catch(e){}},create:()=>{try{const a=new AudioContext();const o=a.createOscillator();const g=a.createGain();o.connect(g);g.connect(a.destination);o.type='sine';o.frequency.setValueAtTime(600,a.currentTime);o.frequency.linearRampToValueAtTime(1200,a.currentTime+0.2);g.gain.value=0.1;o.start();g.gain.exponentialRampToValueAtTime(0.001,a.currentTime+0.3);o.stop(a.currentTime+0.3)}catch(e){}}};
(function(){const _flip=sounds.flip,_win=sounds.win,_create=sounds.create;sounds.flip=function(){try{if(JSON.parse(localStorage.getItem('praedicta_notif_prefs')||'{}').sound!==false)_flip()}catch(e){_flip()}};sounds.win=function(){try{if(JSON.parse(localStorage.getItem('praedicta_notif_prefs')||'{}').sound!==false)_win()}catch(e){_win()}};sounds.create=function(){try{if(JSON.parse(localStorage.getItem('praedicta_notif_prefs')||'{}').sound!==false)_create()}catch(e){_create()}}})();
</script>
<script>
(function(){const faqData=[{q:'👁️ How does PRAEDICTA work?',a:'Seers create praedictions about future events. The community commits YES or NO with virtual PRAE tokens. 46+ APIs auto-resolve the outcome.'},{q:'🤖 How does auto-resolve work?',a:'46+ APIs automatically resolve praedictions: CoinGecko, RedStone, Binance (crypto), Yahoo (stocks), Open-Meteo (weather), TheSportsDB (sports), USGS, NOAA, SpaceX, YouTube, Spotify, Twitch, Reddit, F1, Wikipedia, and many more.'},{q:'📊 What categories exist?',a:'Finance, Politics, Sports, Weather, Entertainment, Technology, Space, Health, Wildcard.'},{q:'🏅 What are Prophet Titles?',a:'Earn reputation ranks: 🌱 Novice (0), 👁️ Seer (100), 👁️‍🗨️ High Seer (250), 🔮 Prophet (500), 🦉 Oracle (1000+).'},{q:'⚖️ Is this gambling?',a:'<strong>NO.</strong> PRAEDICTA is a free prediction game. No real money. No gambling.'},{q:'💰 Can I win real money?',a:'No. PRAE tokens are virtual. They have no cash value. This is for fun and reputation.'},{q:'📱 Can I install as an app?',a:'YES! PWA. Mobile: Chrome → Add to Home Screen. Desktop: click install icon.'}];const c=document.getElementById('faqContainer');if(c)faqData.forEach(i=>{const q=document.createElement('div');q.style.cssText='color:var(--accent);margin-top:16px;font-weight:500;';q.textContent=i.q;const a=document.createElement('div');a.style.cssText='margin-top:4px;color:var(--text-secondary);font-size:.9rem;';a.innerHTML=i.a;c.appendChild(q);c.appendChild(a)})})();
</script>
<script>
(function(){const avatars=['','👁️','🦉','🔮','✨','🐉','🦊','🐺','🦅','🐍','🦋','🐙','🦄','🌋','👻','🧙','🧿','🗿','👧','👨','🧘','💻','📈','📜','📡','📶','🚀','⚽','🏈','⛳','⚾','🏀','🏁','🎾'];const s=document.getElementById('avatarSelect');if(s)s.innerHTML=avatars.map(a=>`<option value="${a}">${a||'No avatar'}</option>`).join('')})();
(function(){const z=[{value:'',label:'Select'},{value:'aries',label:'♈ Aries'},{value:'taurus',label:'♉ Taurus'},{value:'gemini',label:'♊ Gemini'},{value:'cancer',label:'♋ Cancer'},{value:'leo',label:'♌ Leo'},{value:'virgo',label:'♍ Virgo'},{value:'libra',label:'♎ Libra'},{value:'scorpio',label:'♏ Scorpio'},{value:'sagittarius',label:'♐ Sagittarius'},{value:'capricorn',label:'♑ Capricorn'},{value:'aquarius',label:'♒ Aquarius'},{value:'pisces',label:'♓ Pisces'}];const s=document.getElementById('zodiacSelect');if(s)s.innerHTML=z.map(x=>`<option value="${x.value}">${x.label}</option>`).join('')})();
document.addEventListener('click',function(e){const link=e.target.closest('.legal-link');if(!link)return;e.preventDefault();alert('📜 PRAEDICTA is a free prediction game. Virtual tokens only. No real money.')});
</script>
</body>
</html>
